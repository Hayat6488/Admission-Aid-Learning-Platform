import React from 'react';
import Header from '../Shared/Header/Header';

const Blogs = () => {
    return (
        <div>
            <Header></Header>
            <h1>Blogs</h1>
        </div>
    );
};

export default Blogs;